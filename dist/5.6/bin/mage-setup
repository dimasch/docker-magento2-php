#!/bin/sh
chown -R www-data:www-data /var/www
mkdir -p /root/.ssh && cp -fr /var/www/.ssh/* /root/.ssh/ && chown -R www-data:www-data /root/.ssh
su -c "/usr/local/bin/mage-setup-raw" -s /bin/sh www-data
